---
tags:
  - Clip
summary: 'Get a list containing the clips for the supplied track.'
operationId: getTrackClipList
responses:
  '200':
    description: 'Clips fetched.'
    conent:
      application/json:
        schema:
          type: array
          items:
            $ref: '../../../schemas.yaml#/schemas/clipModel'
  '500':
    $ref: '../../../responses.yaml#/500'